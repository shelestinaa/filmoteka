{{ form_start(form) }}
    {{ form_widget(form) }}
<div style="margin-top: 5px">
    <a href="{{ path('index') }}" class="btn btn-dark">< Back to list</a>
    <button class="btn btn-warning">{{ button_label|default('Save') }}</button>
</div>
{{ form_end(form) }}