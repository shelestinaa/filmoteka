{% extends 'base.html.twig' %}

{% block title %}Film{% endblock %}

{% block body %}
    <h1>Film</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Title</th>
            <td>{{ film.title }}</td>
        </tr>
        <tr>
            <th>Year</th>
            <td>{{ film.year }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('index') }}" class="btn btn-dark">< Back to list</a>

    <a href="{{ path('film_edit', {'id': film.id}) }}" class="btn btn-primary">Edit</a>

    {{ include('film/_delete_form.html.twig') }}
{% endblock %}
