{% extends 'base.html.twig' %}

{% block title %}Tag{% endblock %}

{% block body %}
    <h1>Tag</h1>

    <table class="table">
        <tbody>

        <tr>
            <th>Title</th>
            <td>{{ tag.title }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('tag_index') }}" class="btn btn-dark">< Back to list</a>

    <a href="{{ path('tag_edit', {'id': tag.id}) }}" class="btn btn-primary">Edit</a>

    {{ include('tag/_delete_form.html.twig') }}
{% endblock %}
